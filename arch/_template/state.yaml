# Workflow state for this project workspace.
# Owned exclusively by arch-workflow — atomic skills MUST NOT write here.
# Schema: see skills/arch-workflow/references/state-schema.md
#
# This file is a STUB. arch-workflow rewrites it on first run.

template_version: "1.0"
project_name: ""           # filled by arch-workflow from the directory name
created_at: ""             # ISO 8601 timestamp, filled on first run
last_run_at: ""

mode: ""                   # onboard | audit | design | brief
phase: "not-started"       # see state-schema.md for full phase enum
phase_status: "pending"    # pending | in_progress | done | degraded | blocked

baseline_commits: {}       # repo_name -> commit_hash, filled by arch-analyze

overrides: []              # human override audit log; entries are append-only
integrity_history: []      # one entry per integrity check
acceptance_history: []     # one entry per acceptance loop run

degraded: false
degraded_reasons: []
