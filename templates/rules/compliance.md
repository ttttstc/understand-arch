# Compliance Rules

- Mark sensitive data flows with data type, storage location, and transport boundary when evidence exists.
- If evidence is insufficient, create a known unknown instead of inventing compliance posture.
