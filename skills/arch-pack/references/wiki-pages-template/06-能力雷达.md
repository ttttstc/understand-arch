# 06-能力雷达

> 业务能力维度的展开视图。**事实源**: `specs/capabilities.yaml`。本页不重新定义能力,只重组展示。

## 能力总表

按 `importance` 分组(core → supporting → peripheral),同组按 `maturity` 排(mature → evolving → experimental → deprecated → missing)。

### 🔴 Core(挂了业务挂)

| ID | 能力 | 成熟度 | 主承载组件 | 用户感知 | 缺口数 |
|---|---|---|---|---|---|
| CAP-001 | {name} | {maturity} | {primary components} | {y/n} | {N} |

### 🟡 Supporting(挂了业务降级)

| ID | 能力 | 成熟度 | 主承载组件 | 用户感知 | 缺口数 |
|---|---|---|---|---|---|

### 🟢 Peripheral(挂了用户无感)

| ID | 能力 | 成熟度 | 主承载组件 | 用户感知 | 缺口数 |
|---|---|---|---|---|---|

## 能力 × 组件矩阵

> 横向:核心组件;纵向:能力。单元格标 `P=primary / S=secondary / X=shared`。一眼看出**哪些组件承担多个能力**(高耦合候选)、**哪些能力依赖少数组件**(单点风险候选)。

| 能力 \ 组件 | {comp-1} | {comp-2} | {comp-3} | ... |
|---|---|---|---|---|
| CAP-001 {name} | P | S | | |
| CAP-002 {name} | | P | X | |

## 重点能力详情

对每个 `importance=core` 且 `maturity != mature` 的能力,展开一段:

### CAP-NNN: {name}

- **业务意义**: {description}
- **现状成熟度**: {maturity},理由 {一句话}
- **承载组件**: {component} ({role}) / ...
- **外部依赖**: {ext-dep},影响 {缺位时降级路径}
- **当前缺口**:
  - {gap.description} (关联 R-NNN / Q-NNN)
- **演进建议**: 短期 / 中期 / 长期 各一句

## 能力 gap 汇总

> 来源:`capabilities.yaml#capabilities[].gaps[]`,关联到 risks / open_questions。

| 能力 | gap | 关联 risk | 关联 question |
|---|---|---|---|
| CAP-NNN | {description} | R-NNN | Q-NNN |

## Missing capability(识别但未实现)

> `maturity: missing` 的能力清单 — 这是产品/架构师都需要正视的"还没建"事项。

| ID | 能力 | 为何重要 | 阻塞什么 |
|---|---|---|---|
| CAP-NNN | | | |

无 missing 能力时写"无"。

## 演进规划(若有)

> 来自 ADR + CR 的近期能力变化。

| 时间 | 主题 | 涉及能力 | 关联 ADR / CR |
|---|---|---|---|

## 与业界标杆对比(可选)

> 选 1-3 个相关业界产品作能力地图层对比。仅在 `arch-radar` 已跑过对应主题时填,否则跳过本段(写"未做业界对比")。

| 能力 | 我们 | 标杆 A | 标杆 B |
|---|---|---|---|

## 证据回链

- `specs/capabilities.yaml`(本页全部数据源)
- `specs/baseline.yaml#capabilities_index`(能力索引同步)
- `specs/risks.yaml`(gap 关联)
- `specs/decisions.yaml#open_questions`(gap 关联)
