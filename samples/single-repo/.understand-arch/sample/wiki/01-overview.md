# Overview

Sample Order Service is a tiny TypeScript project used to validate understand-arch v3.0 deterministic artifacts.

Design reading order: start with `src/app.ts`, then inspect the `orders` module and its `createOrder` function.
