# v2.0 Acceptance Gates

v2.0 用户入口级 acceptance gate 共 4 个:

- `onboard.yaml`
- `design.yaml`
- `audit.yaml`
- `wiki.yaml`

`arch-diagram` 在 v2.0 是占位入口,不设置独立 gate。旧 `brief.yaml` 已废弃,受众化输出并入 `arch-wiki`。

