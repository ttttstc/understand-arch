id: REG-V2-000
title: "v2 regression case"
workspace: ".understand-arch/sample"
inputs:
  repos_yaml: "specs/repos.yaml"
expected:
  files:
    - "specs/cross-repo.json"
    - "wiki/README.md"
  invariants:
    - "no arch/ workspace"
    - "node ids use repo_id prefix"
    - "cross repo edges live in cross-repo.json"

