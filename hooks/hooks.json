{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "printf '%s' \"$TOOL_INPUT\" | grep -qE 'git\\s+(commit|merge|cherry-pick|rebase)' && [ -f .understand-arch/*/state.yaml ] && grep -q 'hooks_enabled:[[:space:]]*true' .understand-arch/*/state.yaml && [ -f .understand-arch/*/specs/arch-layer.json ] && echo \"[understand-arch] Git history changed. Read ${CLAUDE_PLUGIN_ROOT}/hooks/arch-update-prompt.md and refresh the affected graph/arch-layer/wiki outputs in this Claude session.\" || true"
          }
        ]
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -f .understand-arch/*/state.yaml ] && grep -q 'hooks_enabled:[[:space:]]*true' .understand-arch/*/state.yaml && [ -f .understand-arch/*/specs/arch-layer.json ] && echo \"[understand-arch] Session started with hooks enabled. Read ${CLAUDE_PLUGIN_ROOT}/hooks/arch-update-prompt.md if graph freshness differs from the current git commit.\" || true"
          }
        ]
      }
    ]
  }
}
